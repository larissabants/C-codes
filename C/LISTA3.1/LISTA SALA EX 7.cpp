#include<stdio.h>
#include<conio.h>
#include<math.h>

int main()
{
	int a, b, c;
	for (a>0; a<=15; ++a)
	if(c=pow(3,a))
	printf("%d elevado a: %d = %d \n", 3, a, c);
	
	getch();
	return 0;
	
}

#include <stdio.h>
#include <conio.h>

 int main()
{
 	int a, b=0;
	for(a=1; a<=100; ++a)
 	b = b + a;
	printf("A soma dos 100 primeiros numeros inteiros eh: %d\n", b);
 
 	return 0;
 	
 }
